# Trellix
Frontend for Trellix — a software development project manager to organize dev projects, link repos, and track delivery. API contract: backend OpenAPI docs at your configured `VITE_API_URL` (e.g. `/docs`).
